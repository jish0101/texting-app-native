import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    marginTop: 50,
    width: "100%",
  },
  logoIcon: { width: 200, height: 200, marginBottom: 20 },
  button: {
    width: "80%",
    borderRadius: 10,
    marginTop: 20,
  },
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
  },
});
